services:
  wallet-to-actualbudget:
    image: ghcr.io/zhria/wallet-to-actualbudget:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./config/mapping.json:/app/config/mapping.json:ro
      - actual-data:/app/actual-data

volumes:
  actual-data:
